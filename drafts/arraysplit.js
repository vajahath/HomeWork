var index = unUsedPin.indexOf(device_id_mapping[addingDevice]);
                    console.log(index);
                    if (index > -1) {
                        array.splice(0, 1);
                    }